{
  "name": "cws-bridge",
  "version": "2.0.0",
  "description": "cws: multi-session WebSocket bridge over Claude Code / Codex CLI with WebUI (Node.js rewrite of cc-bridge)",
  "type": "module",
  "private": true,
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node wsclient.mjs",
    "build:webui": "npm --prefix webui-src install --no-fund --no-audit && npm --prefix webui-src run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@openclaw/gateway-client": "^2026.9.4",
    "@openclaw/gateway-protocol": "^2026.9.4",
    "ws": "^8.18.0"
  }
}
